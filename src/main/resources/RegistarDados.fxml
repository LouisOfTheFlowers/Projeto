<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.Cursor?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="Test.RegistarDadosController"
      spacing="25" alignment="TOP_CENTER"
      style="-fx-background-color: #f0f8f0; -fx-padding: 30; -fx-font-family: 'Segoe UI', sans-serif;">

    <!-- Cabeçalho com botão de voltar -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Button fx:id="backButton" onAction="#goBack"
                text="Voltar"
                style="-fx-background-color: transparent; -fx-text-fill: #2e8b57; -fx-font-size: 14px;">
            <cursor><Cursor fx:constant="HAND"/></cursor>
            <graphic>
                <ImageView fx:id="backIcon" fitHeight="20" fitWidth="20" preserveRatio="true">
                    <Image url="Icons/back.png"/>
                </ImageView>
            </graphic>
        </Button>
    </HBox>

    <Label text="📝 Registar Dados"
           style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: #2e8b57;"/>

    <!-- Formulário -->
    <VBox spacing="15" alignment="CENTER">
        <TextField fx:id="campoNomeField" promptText="Nome Completo" prefWidth="300" style="-fx-font-size: 14px;"/>
        <TextField fx:id="campoIdadeField" promptText="Idade" prefWidth="300" style="-fx-font-size: 14px;"/>
        <TextField fx:id="campoEnderecoField" promptText="Endereço" prefWidth="300" style="-fx-font-size: 14px;"/>
        <TextField fx:id="campoTelefoneField" promptText="Telefone" prefWidth="300" style="-fx-font-size: 14px;"/>
    </VBox>

    <!-- Botão Submeter -->
    <Button text="Guardar Dados"
            fx:id="guardarButton"
            onAction="#handleGuardar"
            style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px; -fx-padding: 10 40; -fx-background-radius: 8;"
            prefWidth="220"/>
</VBox>
